import * as React from 'react';
import { View } from 'react-native';

export function PreviewPanel(_props: {
    sessionId: string;
    machineId: string;
    onClose: () => void;
}) {
    return <View />;
}
